= Data Mover 超时标记
:allow-uri-read: 




== Data Mover 超时标记

使用注释 `protect.trident.netapp.io/data-mover-timeout-sec`在备份和还原期间设置每个操作的数据移动器超时(秒)。它指定数据移动器在超时之前等待操作取得进展的时间。这对于检测和处理停滞的传输（例如，当网络问题阻止卷数据还原进度时）非常有用。

您可以在 YAML 文件中使用此注释，如下所示：

[source, yaml]
----
---
apiVersion: protect.trident.netapp.io/v1
kind: BackupRestore
metadata:
  name: bkp-restore-kopia
  namespace: trident-protect
  annotations: # Optional annotations to configure data movement timeout
    protect.trident.netapp.io/data-mover-timeout-sec: "2"
spec:
  appVaultRef: appvault-name
  appArchivePath: my-backup-path
  namespaceMapping: [{"source": "my-source-namespace", "destination": "my-destination-namespace"}]
  storageClassMapping:
    destination: "${destinationStorageClass}"
    source: "${sourceStorageClass}"
----
在此示例中，如果还原操作在 2 秒内未发生任何进度，则注释将使 `BackupRestore`操作失败。


NOTE: 如果批注值小于或等于 0，则操作将立即失败。
